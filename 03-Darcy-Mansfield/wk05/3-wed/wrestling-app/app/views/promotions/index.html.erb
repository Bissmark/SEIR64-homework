 <h1>All Promotions</h1>

<% @promotions.each do |promotion| %>
    <%= image_tag promotion.logo, :class => 'logo' if promotion.logo.present? %>
    <div class="promotion"><%= link_to promotion.promotion, promotion %></div>
<% end %>