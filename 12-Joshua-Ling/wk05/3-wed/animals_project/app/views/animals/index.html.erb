<h1>All Animals</h1>

<% @animals.each do |animal|%>
    <%= link_to image_tag(animal.image, :class => 'thumb'),animal if animal.image.present? %>
    <%= link_to image_tag("https://placekitten.com/200/300"),animal unless animal.image.present? %>

<% end %>