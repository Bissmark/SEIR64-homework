<h1>All Directors</h1>

<% @directors.each do |director| %>

    <%= image_tag director.image, :class => 'thumb' if director.image.present? %>
    <h3> <%= link_to director.name, director %> </h3>

<% end %>